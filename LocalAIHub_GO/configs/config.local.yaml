server:
  host: 0.0.0.0
  port: 3334

security:
  admin_session_secret: localaihub-admin-session-secret
  encryption_key: localaihub-encryption-key
  admin_password_plain: yu3209605851

database:
  driver: mysql
  dsn: root:admin123@tcp(127.0.0.1:3306)/localaihub?charset=utf8mb4&parseTime=True&loc=UTC
  init_sql_path: sql/init.sql
  max_open_conns: 10
  min_idle_conns: 2
  max_conn_lifetime_minutes: 30
  max_conn_idle_minutes: 10

cors:
  allowed_origins:
    - http://127.0.0.1:3000
    - http://localhost:3000

redis:
  addr: 127.0.0.1:6379
  password: ""
  db: 0
